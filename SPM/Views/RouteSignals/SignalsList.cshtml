@model IEnumerable<MOE.Common.Models.ATSPM_Signals>

<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SignalDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SignalDescription)
        </td>
    </tr>
}

</table>